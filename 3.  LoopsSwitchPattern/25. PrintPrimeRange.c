#include <stdio.h>
int main() {
    int n, m, i = 1;;
    printf("Enter the first number: ");
    scanf("%d", &n);
    printf("Enter the second number: ");
    scanf("%d", &m);
    printf("Prime numbers between %d and %d are:\n", n, m);
    printf("\n");
    return 0;
}